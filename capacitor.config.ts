import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.proceum.app',
  appName: 'Proceum',
  webDir: 'dist/ProceumWebApp',
  bundledWebRuntime: false
};

export default config;
